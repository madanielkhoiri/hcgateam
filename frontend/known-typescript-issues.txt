# Utang teknis TypeScript yang sudah ada SEBELUM CI ini dibuat (2026-08-30).
# Format: <path-file>::<kode-error>
# Dipakai oleh scripts/check-typescript.mjs supaya CI tidak gagal karena ini,
# tapi tetap gagal kalau ada error BARU di file lain / kode error lain.
# Kalau salah satu baris ini sudah diperbaiki, hapus barisnya dari sini.
